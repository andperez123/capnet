# TrustGraph — Verification, reputation, governance
# Copy to .env and fill values

# Environment: dev | pilot | prod
CAPNET_ENV=dev

# Server
PORT=4100
HOST=0.0.0.0

# API key required for write endpoints (emit events, attestations)
TRUSTGRAPH_API_KEY=

# Persistence (append-only event store + derived scores)
# DATABASE_URL=postgresql://user:pass@localhost:5432/trustgraph

# Optional: audit log export
# AUDIT_LOG_PATH=/var/log/trustgraph/audit.jsonl
