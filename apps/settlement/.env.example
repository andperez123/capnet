# Settlement — Capital flow (Stripe + Coinbase)
# Copy to .env and fill values

# Environment: dev | pilot | prod
CAPNET_ENV=dev

# Server
PORT=4300
HOST=0.0.0.0

# TrustGraph (proof + threshold checks before release)
TRUSTGRAPH_URL=http://localhost:4100
TRUSTGRAPH_API_KEY=

# Capnet API (optional: resolve payee/operator)
CAPNET_API_URL=http://localhost:4200
CAPNET_API_KEY=

# Stripe (fiat)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Coinbase (crypto / Agent Wallet)
COINBASE_API_KEY=
COINBASE_API_SECRET=
# COINBASE_AGENT_WALLET_ID=

# Optional: persistence for intents and release history
# DATABASE_URL=postgresql://user:pass@localhost:5432/settlement
