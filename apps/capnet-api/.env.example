# Capnet API — Waitlist, registry, activation, policies
# Copy to .env and fill values

# Environment: dev | pilot | prod
CAPNET_ENV=dev

# Server
PORT=4200
HOST=0.0.0.0

# API key for admin / server-to-server
CAPNET_API_KEY=

# WakeNet + TrustGraph + Settlement URLs (for orchestration)
WAKENET_URL=http://localhost:4000
TRUSTGRAPH_URL=http://localhost:4100
SETTLEMENT_URL=http://localhost:4300

# API keys for outbound calls to other Capnet services
WAKENET_API_KEY=
TRUSTGRAPH_API_KEY=
SETTLEMENT_API_KEY=

# Database (waitlist, agents, operators, activation state)
# DATABASE_URL=postgresql://user:pass@localhost:5432/capnet_api
