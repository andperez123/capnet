# WakeNet — Operations & execution layer
# Copy to .env and fill values

# Environment: dev | pilot | prod
CAPNET_ENV=dev

# Server
PORT=4000
HOST=0.0.0.0

# TrustGraph API (for emitting signal-received / delivery events)
TRUSTGRAPH_URL=http://localhost:4100
TRUSTGRAPH_API_KEY=

# Capnet API (for agent registry / webhook targets)
CAPNET_API_URL=http://localhost:4200
CAPNET_API_KEY=

# Optional: persistence for subscriptions and delivery state
# DATABASE_URL=postgresql://user:pass@localhost:5432/wakenet
