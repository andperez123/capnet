# Capnet API (repo root — used by api/* and lib/*)
# Copy to .env for local dev; set in Vercel for production.
# Full list: docs/ENV-REFERENCE.md

# ─── Required for production (persistent data) ─────────────────────
# Vercel KV or Upstash Redis. Without these, store is in-memory (resets on cold start).
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ─── Recommended for production ───────────────────────────────────
CAPNET_ENV=prod
CAPNET_API_URL=https://capnet.work

# ─── Optional: identity default namespace (default: praxis) ────────
IDENTITY_NAMESPACE=praxis

# ─── TrustGraph (for status + event push + score proxy) ─────────────
# Required for trustgraph.status: ok and agent event push
TRUSTGRAPH_URL=https://trustgraph-production.up.railway.app
# TRUSTGRAPH_API_KEY=           # if TrustGraph requires Bearer auth
# TRUSTGRAPH_WEBHOOK_SECRET=    # if TrustGraph validates webhook signature

# ─── Optional: other services (not needed for current API) ───────────
WAKENET_URL=
WAKENET_API_KEY=
SETTLEMENT_URL=
SETTLEMENT_API_KEY=
