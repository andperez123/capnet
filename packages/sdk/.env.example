# Capnet SDK — client configuration
# Copy to .env when integrating against Capnet services

# Environment: dev | pilot | prod
CAPNET_ENV=dev

# Base URLs (hosted or local)
CAPNET_API_URL=http://localhost:4200
WAKENET_URL=http://localhost:4000
TRUSTGRAPH_URL=http://localhost:4100
SETTLEMENT_URL=http://localhost:4300

# API keys (write access where required)
CAPNET_API_KEY=
WAKENET_API_KEY=
TRUSTGRAPH_API_KEY=
SETTLEMENT_API_KEY=
